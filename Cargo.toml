[workspace]
members = ["rust"]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
# panic must unwind: PyO3 converts panics into Python exceptions; abort
# would kill the whole host process instead
strip = true
overflow-checks = false
